# CSRF: admin/client forms use session-backed tokens (reliable after login + Turbo).
# Stateless IDs only for logout (and similar) — NOT "submit" (breaks Create Service / edit forms).
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - logout
